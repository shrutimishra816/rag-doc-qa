# Copy this to .env.local for local development
# The Vite proxy handles /api → http://localhost:8000 automatically in dev mode
# Only set this for production deployments pointing to your Render backend URL

VITE_API_URL=https://your-backend-url.onrender.com
